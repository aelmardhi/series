const fs = require('fs');

loadConfigs(){
    const Configs = [];
    

} 
const Configs = loadConfigs()


switch (process.argv[2]) { //switch Command
    case 'set':
        
        break;

    default:
        break;
}

